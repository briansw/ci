<div class="course-info">
  Core Interaction<br />
  2014
  wattersb@newschool.edu
</div>

<div class="students">
  <ul>
    <% @course.students.each do |student| %>
      <li><%= link_to(student.full_name, student.url, target: '_blank') %></li>
    <% end %>
  </ul>
</div>