<div class="content-block <%= css_class(cb) %>" id="<%= css_id(cb) %>">
  <%= image_tag(cb.image.attachment_url(:c1), class: 'lightboxable', alt: strip_tags(cb.caption), data: { id: css_lightbox_id(cb), hires: cb.image.attachment_url(:c4), caption: ft(cb.caption) }) %>
  <div class="caption">
    <%= ft(cb.caption) %>
  </div>
</div>